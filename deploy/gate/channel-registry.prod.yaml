# Production gate on relay-2 — llama.cpp in docker network
permitted_channels:
  - local-llm

forbidden_exports:
  - cloud-telemetry
  - training-retention

model_allowlist:
  - qwen2.5-0.5b-instruct

delta_t_secs: 300

llama_cpp_url: http://llama:8080
