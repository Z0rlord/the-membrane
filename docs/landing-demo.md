# Membrane landing demo (deprecated path)

The public product dashboard documentation moved to **[demo.md](demo.md)**.

Use the canonical command:

```bash
cargo run -p membrane-cli -- demo
```

The old `membrane landing-demo` command remains a hidden, deprecated alias for compatibility and prints a migration warning.
