# Attestable local demo (retired name — redirect)

Moved to **[demo.md](demo.md)** for **The Membrane** product dashboard.

```bash
cargo run -p membrane-cli -- demo
```

> This file is an intentional retired-name redirect tombstone. The old `membrane landing-demo` command remains a hidden, deprecated alias; the technical relay test is now `membrane iac-smoke`.
