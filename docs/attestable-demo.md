# Attestable local demo (retired name — redirect)

Moved to **[landing-demo.md](landing-demo.md)** for **The Membrane** product dashboard.

```bash
cargo run -p membrane-cli -- landing-demo
```

> `membrane demo` is a separate relay IAC smoke test, not the product dashboard.
